resource "azurerm_container_registry" "main" {
    name                = "service"
    resource_group_name = resource_group_name = azurerm_resource_group.main.name
    location            = azurerm_resource_group.main.location
    sku                 = "Basic"
}

output "acr" {
  sensitive = true
  value     = azurerm_container_registry.main
}